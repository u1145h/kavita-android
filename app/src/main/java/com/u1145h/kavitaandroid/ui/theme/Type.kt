package com.u1145h.kavitaandroid.ui.theme

import androidx.compose.material3.Typography

/**
 * Base Material 3 typography. Font sizes use sp so they scale with the system
 * font size setting for accessibility.
 */
val KavitaTypography = Typography()
